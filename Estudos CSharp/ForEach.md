- Sintaxe utilizada para percorrer arrays(seja lista ou vec);
- Leitura: "Para cada objeto 'obj' contido em vect, faça:"
```
string[] vect = new string[]{"Maria", "Bob", "Alex"};

foreach(string obj in vect){
	Console.WriteLine(obj);
}
```